<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.miniprojet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ReunionDetails" android:parentActivityName=".Historique"></activity>
        <activity
            android:name=".Historique"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".AjouterSujets"
            android:parentActivityName=".CreerReunion" />
        <activity
            android:name=".AjouterPersonne"
            android:parentActivityName=".CreerReunion" />
        <activity
            android:name=".CreerReunion"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>